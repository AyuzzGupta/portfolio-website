# Portfolio Implementation Tasks

- [x] Initialize React + Tailwind project & configure base styles (`index.html`, `tailwind.config.js`, `index.css`)
- [x] Copy PDF certificates and resume to `public` folder
- [x] Create `src/config/certificates.json`
- [x] Build the Three.js Background (`ThreeBackground.jsx`) with 3D wireframe shapes & starfield
- [x] Implement `Navbar` (sticky, mobile menu, CV download)
- [x] Build `Hero` (tagline, intro, count-up stats on scroll)
- [x] Build `Portfolio` & `Skills` sections (using actual resume content)
- [x] Build `LeetCode` & `Experience` timelines (timeline, 1M1B internship, AI/ML course)
- [x] Build `Education` timeline & `Certifications` grid (features pinned certificates first)
- [x] Build `Connect` footer
- [x] Verify everything works (clean build, responsive navigation, interactions)
